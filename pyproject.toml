[project]
name = "dvc-databricks"
version = "1.0.0"
description = "DVC remote plugin for Databricks Unity Catalog Volumes"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Óscar Reyes" }]
keywords = ["dvc", "databricks", "data-versioning", "mlops", "unity-catalog"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
requires-python = ">=3.10"
dependencies = [
    "databricks-sdk>=0.40.0",
    "dvc>=3.0",
    "dvc-objects>=5.0",
    "fsspec",
]

[project.urls]
Homepage = "https://github.com/ogreyesp/dvc-databricks"
Repository = "https://github.com/ogreyesp/dvc-databricks"
Issues = "https://github.com/ogreyesp/dvc-databricks/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dvc_databricks"]

[tool.hatch.build.hooks.custom]
# hatch_build.py installs dvc_databricks_startup.pth into site-packages.
# Python executes .pth files at startup, which imports dvc_databricks and
# registers dbvol:// into dvc_objects.known_implementations automatically.

[tool.semantic_release]
version_toml = ["pyproject.toml:project.version"]
branch = "main"
changelog_file = "CHANGELOG.md"
build_command = "pip install hatchling && python -m hatchling build"
dist_path = "dist/"
upload_to_pypi = true
upload_to_release = true
commit_message = "chore(release): {version} [skip ci]"

[tool.semantic_release.commit_parser_options]
allowed_tags = [
    "feat",
    "fix",
    "perf",
    "refactor",
    "docs",
    "chore",
    "style",
    "test",
    "build",
    "ci",
]
minor_tags = ["feat"]
patch_tags = ["fix", "perf", "refactor"]

[tool.semantic_release.remote]
type = "github"

[tool.semantic_release.publish]
dist_glob_patterns = ["dist/*"]
upload_to_vcs_release = true
